- defaults:
    link:
      relink: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  
- clean: ['~']

- link:
    # zsh
    ~/.zshrc: zshrc
    # tmux
    ~/.tmux.conf : tmux.conf
    #p10k
    ~/.p10k.zsh : p10k.zsh
    #git
    ~/.gitconfig : gitconfig
